name := "chapter3"

version := "0.1"

scalaVersion := "2.12.4"

libraryDependencies += "org.scalatest" %% "scalatest" % "3.0.4" % "test"
libraryDependencies += "org.typelevel" %% "cats-core" % "1.0.1"
scalacOptions += "-Ypartial-unification"
# mancano ancora pochi file alla fine ufficiale, il solo 2 file in SCSS e modificare
# la UI è l'app è completa ! 
